"use client";

import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { Bold, Download, Eraser, FileDown, MousePointer2, Redo2, RectangleHorizontal, RotateCcw, Trash2, Type, Undo2, ZoomIn, ZoomOut } from "lucide-react";
import { downloadEditedPdfs, loadPdfJs, type PdfOverlay } from "../../../../src/services/pdf";
import { FileDropzone } from "../../components/FileDropzone";
import { PdfCanvas } from "../../components/PdfCanvas";
import { useApp } from "../../state/AppContext";

type Tool="select"|"text"|"editText"|"erase"|"rect";
type EditorDoc={id:string;file:File;pageCount:number;page:number;past:PdfOverlay[][];present:PdfOverlay[];future:PdfOverlay[][]};

export function PdfEditor(){const app=useApp();const[docs,setDocs]=useState<EditorDoc[]>([]);const[activeId,setActiveId]=useState("");const[tool,setTool]=useState<Tool>("select");const[zoom,setZoom]=useState(80);const[text,setText]=useState("Novo texto");const[fontSize,setFontSize]=useState(12);const[color,setColor]=useState("#1f1f1f");const[bold,setBold]=useState(false);const[selected,setSelected]=useState("");const stage=useRef<HTMLDivElement>(null);const dragStart=useRef<{x:number;y:number}|null>(null);const active=docs.find(doc=>doc.id===activeId);
  const updateDoc=useCallback((fn:(doc:EditorDoc)=>EditorDoc)=>setDocs(current=>current.map(doc=>doc.id===activeId?fn(doc):doc)),[activeId]);
  const commit=useCallback((next:PdfOverlay[])=>updateDoc(doc=>({...doc,past:[...doc.past,doc.present],present:next,future:[]})),[updateDoc]);
  const addFiles=async(files:File[])=>{const created:EditorDoc[]=[];for(const file of files){try{const pdfjs=await loadPdfJs();const pdf=await pdfjs.getDocument({data:await file.arrayBuffer()}).promise;created.push({id:crypto.randomUUID(),file,pageCount:pdf.numPages,page:0,past:[],present:[],future:[]})}catch(error){app.toast("error",`Não foi possível abrir ${file.name}`,error instanceof Error?error.message:undefined)}}setDocs(current=>[...current,...created]);if(!activeId&&created[0])setActiveId(created[0].id)};
  const undo=useCallback(()=>updateDoc(doc=>doc.past.length?{...doc,present:doc.past.at(-1)!,past:doc.past.slice(0,-1),future:[doc.present,...doc.future]}:doc),[updateDoc]);
  const redo=useCallback(()=>updateDoc(doc=>doc.future.length?{...doc,past:[...doc.past,doc.present],present:doc.future[0],future:doc.future.slice(1)}:doc),[updateDoc]);
  const removeSelected=useCallback(()=>{if(!active||!selected)return;commit(active.present.filter(item=>item.id!==selected));setSelected("")},[active,selected,commit]);
  useEffect(()=>{const key=(event:KeyboardEvent)=>{if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="z"){event.preventDefault();undo()}else if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="y"){event.preventDefault();redo()}else if(event.key==="Delete")removeSelected();else if(event.key==="Escape")setTool("select")};window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key)},[undo,redo,removeSelected]);
  const pointer=(event:React.PointerEvent<HTMLDivElement>)=>{if(!active||!stage.current||tool==="select")return;const rect=stage.current.getBoundingClientRect();const point={x:(event.clientX-rect.left)/rect.width,y:(event.clientY-rect.top)/rect.height};if(event.type==="pointerdown"){if(tool==="text"||tool==="editText"){const overlay:PdfOverlay={id:crypto.randomUUID(),page:active.page,type:"text",x:point.x,y:point.y,width:.25,height:.055,text,fontSize,color,bold};const next=tool==="editText"?[...active.present,{...overlay,id:crypto.randomUUID(),type:"erase" as const},overlay]:[...active.present,overlay];commit(next);setSelected(overlay.id);setTool("select")}else dragStart.current=point}else if(event.type==="pointerup"&&dragStart.current){const start=dragStart.current;dragStart.current=null;const overlay:PdfOverlay={id:crypto.randomUUID(),page:active.page,type:tool==="erase"?"erase":"rect",x:Math.min(start.x,point.x),y:Math.min(start.y,point.y),width:Math.abs(point.x-start.x),height:Math.abs(point.y-start.y),color};if(overlay.width>.005&&overlay.height>.005)commit([...active.present,overlay]);}};
  const currentOverlays=active?.present.filter(item=>item.page===active.page)??[];const selectedOverlay=active?.present.find(item=>item.id===selected);
  const updateSelected=(patch:Partial<PdfOverlay>)=>{if(!active||!selected)return;commit(active.present.map(item=>item.id===selected?{...item,...patch}:item))};
  const download=async(all:boolean)=>{if(!active)return;try{const targets=all?docs:[active];await downloadEditedPdfs(targets.map(doc=>({file:doc.file,overlays:doc.present})),all);app.addAudit({operation:all?"Baixou todos os PDFs editados":"Baixou PDF editado",module:"Editor de PDF",result:all?`${docs.length} arquivos em ZIP`:active.file.name,status:"SUCESSO",processedCount:all?docs.length:1});app.toast("success",all?"ZIP com PDFs separados gerado":"PDF editado baixado")}catch(error){app.toast("error","Falha ao exportar PDF",error instanceof Error?error.message:undefined)}};
  if(!docs.length)return <><div className="page-heading compact"><div><h2>Editor de PDF</h2><p>Cada documento mantém suas próprias páginas, edições e histórico.</p></div></div><section className="surface import-surface"><div className="import-form"><FileDropzone accept=".pdf" multiple files={[]} onFiles={addFiles}/></div></section></>;
  return <div className="pdf-editor"><div className="editor-heading"><div><h2>Editor de PDF</h2><p>{active?.file.name} · página {(active?.page??0)+1} de {active?.pageCount}</p></div><div><button className="button secondary" onClick={()=>download(false)}><Download/> Baixar atual</button><button className="button primary" onClick={()=>download(true)}><FileDown/> Baixar todos</button></div></div><div className="editor-toolbar"><div className="tool-group"><ToolButton active={tool==="select"} label="Selecionar" onClick={()=>setTool("select")} icon={<MousePointer2/>}/><ToolButton active={tool==="text"} label="Adicionar texto" onClick={()=>setTool("text")} icon={<Type/>}/><ToolButton active={tool==="editText"} label="Editar texto" onClick={()=>setTool("editText")} icon={<RotateCcw/>}/><ToolButton active={tool==="erase"} label="Apagar área" onClick={()=>setTool("erase")} icon={<Eraser/>}/><ToolButton active={tool==="rect"} label="Retângulo" onClick={()=>setTool("rect")} icon={<RectangleHorizontal/>}/></div><div className="tool-options"><input className="toolbar-text" value={text} onChange={event=>setText(event.target.value)} aria-label="Texto a inserir"/><select value={fontSize} onChange={event=>setFontSize(Number(event.target.value))}>{[8,9,10,11,12,14,16,18,24,32].map(value=><option key={value}>{value}</option>)}</select><input type="color" value={color} onChange={event=>setColor(event.target.value)} aria-label="Cor"/><button className={bold?"active":""} onClick={()=>setBold(value=>!value)}><Bold/></button><button disabled={!active?.past.length} onClick={undo}><Undo2/></button><button disabled={!active?.future.length} onClick={redo}><Redo2/></button><button disabled={!selected} onClick={removeSelected}><Trash2/></button></div><div className="zoom-controls"><button onClick={()=>setZoom(value=>Math.max(40,value-10))}><ZoomOut/></button><span>{zoom}%</span><button onClick={()=>setZoom(value=>Math.min(150,value+10))}><ZoomIn/></button></div></div><div className="editor-body"><aside className="editor-sidebar"><div className="editor-side-title"><strong>PDFs carregados</strong><label>+<input type="file" accept=".pdf" multiple hidden onChange={event=>addFiles([...(event.target.files??[])])}/></label></div><div className="editor-doc-list">{docs.map(doc=><button className={doc.id===activeId?"active":""} key={doc.id} onClick={()=>{setActiveId(doc.id);setSelected("")}}><span className="document-icon">PDF</span><span><strong>{doc.file.name}</strong><small>{doc.pageCount} página(s)</small></span><i className={doc.present.length?"dirty":""}>{doc.present.length?"Editado":"Sem alterações"}</i></button>)}</div><div className="editor-side-title"><strong>Páginas</strong></div><div className="editor-page-list">{active&&Array.from({length:active.pageCount},(_,index)=><button className={active.page===index?"active":""} key={index} onClick={()=>updateDoc(doc=>({...doc,page:index}))}><div><PdfCanvas file={active.file} pageNumber={index+1} scale={.13}/>{active.present.some(item=>item.page===index)&&<i/>}</div><span>Página {index+1}</span></button>)}</div></aside><section className="editor-workspace"><div className={`pdf-stage tool-${tool}`} ref={stage} style={{width:`${zoom}%`}} onPointerDown={pointer} onPointerUp={pointer}>{active&&<PdfCanvas file={active.file} pageNumber={active.page+1} scale={1.35} className="main-pdf-canvas"/>}<div className="overlay-layer">{currentOverlays.map(overlay=><button key={overlay.id} className={`pdf-overlay ${overlay.type} ${selected===overlay.id?"selected":""}`} style={{left:`${overlay.x*100}%`,top:`${overlay.y*100}%`,width:`${overlay.width*100}%`,height:`${overlay.height*100}%`,borderColor:overlay.color,color:overlay.color,fontSize:`${overlay.fontSize??12}px`,fontWeight:overlay.bold?700:400}} onPointerDown={event=>{if(tool==="select"){event.stopPropagation();setSelected(overlay.id)}}}>{overlay.type==="text"?overlay.text:""}</button>)}</div></div>{selectedOverlay&&<div className="element-properties"><strong>Propriedades do elemento</strong><label>X <input type="number" step=".01" value={selectedOverlay.x.toFixed(2)} onChange={event=>updateSelected({x:Number(event.target.value)})}/></label><label>Y <input type="number" step=".01" value={selectedOverlay.y.toFixed(2)} onChange={event=>updateSelected({y:Number(event.target.value)})}/></label><label>Largura <input type="number" step=".01" value={selectedOverlay.width.toFixed(2)} onChange={event=>updateSelected({width:Number(event.target.value)})}/></label><label>Altura <input type="number" step=".01" value={selectedOverlay.height.toFixed(2)} onChange={event=>updateSelected({height:Number(event.target.value)})}/></label>{selectedOverlay.type==="text"&&<label>Texto <input value={selectedOverlay.text} onChange={event=>updateSelected({text:event.target.value})}/></label>}</div>}</section></div></div>;
}

function ToolButton({active,label,onClick,icon}:{active:boolean;label:string;onClick:()=>void;icon:React.ReactNode}){return <button className={active?"active":""} onClick={onClick}>{icon}<span>{label}</span></button>}
