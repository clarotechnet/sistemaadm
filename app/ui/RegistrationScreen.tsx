"use client";

import { useState } from "react";
import { ArrowLeft, CheckCircle2, UserPlus } from "lucide-react";
import type { UserProfile } from "../../src/types";

export function RegistrationScreen({profile}:{profile:UserProfile}){const[form,setForm]=useState({name:profile.name,department:profile.department,jobTitle:profile.jobTitle});const[busy,setBusy]=useState(false);const[done,setDone]=useState(false);const[error,setError]=useState("");const save=async()=>{if(!form.name||!form.department||!form.jobTitle){setError("Preencha nome, setor e cargo.");return}setBusy(true);setError("");const response=await fetch("/api/profile",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(form)});setBusy(false);if(response.ok)setDone(true);else setError("Não foi possível salvar o cadastro.")};return <main className="registration-page"><a href="/" className="back-link"><ArrowLeft/> Voltar</a><section className="registration-card">{done?<div className="registration-success"><CheckCircle2/><h1>Cadastro concluído</h1><p>{profile.status==="AGUARDANDO APROVAÇÃO"?"Seu acesso está aguardando aprovação de um administrador.":"Seu perfil foi atualizado com sucesso."}</p><a className="button primary" href="/">Ir para o RH Control</a></div>:<><span className="auth-form-icon"><UserPlus/></span><h1>Complete seu cadastro</h1><p>Os dados abaixo identificam seu perfil nas operações e logs de auditoria.</p><label className="field"><span>Nome completo</span><input value={form.name} onChange={event=>setForm({...form,name:event.target.value})}/></label><label className="field"><span>E-mail</span><input value={profile.email} disabled/></label><div className="two-fields"><label className="field grow"><span>Setor</span><input value={form.department} onChange={event=>setForm({...form,department:event.target.value})}/></label><label className="field grow"><span>Cargo</span><input value={form.jobTitle} onChange={event=>setForm({...form,jobTitle:event.target.value})}/></label></div>{error&&<p className="inline-error">{error}</p>}<button className="button primary full" disabled={busy} onClick={save}>{busy?"Salvando...":"Concluir cadastro"}</button><small>Seu perfil inicial: {profile.role}. Status: {profile.status}.</small></>}</section></main>}
